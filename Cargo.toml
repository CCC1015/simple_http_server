[package]
name = "simple_http_server"
version = "0.1.0"
edition = "2021"

[dependencies]
hyper = { version = "0.14.32", features = ["http1", "server", "tcp"] }
tokio = { version = "1", features = ["full"] }
